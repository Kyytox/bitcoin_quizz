interface LNBitsConfig {
    adminKey: string;
    invoiceReadKey: string;
    endpoint?: string;
}
interface Paywalls {
    amount: number;
    description: string;
    extras: string;
    id: string;
    memo: string;
    remembers: boolean;
    time: number;
    url: string;
    wallet: string;
}
interface CreatePaywall {
    amount: number;
    description: string;
    extras: string;
    id: string;
    memo: string;
    remembers: boolean;
    time: number;
    url: string;
    wallet: string;
}
interface Invoice {
    payment_hash: string;
    payment_request: string;
}
interface InvoiceCheck {
    paid: string;
}
export declare class LNBitsPaywallClass {
    private adminKey;
    private invoiceReadKey;
    private endpoint;
    private api;
    constructor(params: LNBitsConfig);
    getPaywalls: () => Promise<Paywalls[]>;
    createPaywall: (params: {
        amount: number;
        description: string;
        memo: string;
        remembers: boolean;
        url: string;
    }) => Promise<CreatePaywall>;
    createInvoice: (params: {
        paywall_id: string;
        amount: number;
    }) => Promise<Invoice>;
    checkInvoice: (params: {
        paywall_id: string;
        payment_hash: string;
    }) => Promise<InvoiceCheck>;
    deletePaywall: (params: {
        paywall_id: string;
    }) => Promise<boolean>;
}
export {};
