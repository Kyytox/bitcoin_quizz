interface LNBitsConfig {
    adminKey: string;
    invoiceReadKey: string;
    endpoint?: string;
}
interface TPoS {
    currency: string;
    id: string;
    name: string;
    wallet: string;
}
export declare class LNBitsTPoSClass {
    private adminKey;
    private invoiceReadKey;
    private endpoint;
    private api;
    constructor(params: LNBitsConfig);
    getTPoS: () => Promise<TPoS[]>;
    createTPoS: (params: {
        name: string;
        currency: string;
    }) => Promise<TPoS>;
    deleteTPoS: (params: {
        tpos_id: string;
    }) => Promise<boolean>;
}
export {};
