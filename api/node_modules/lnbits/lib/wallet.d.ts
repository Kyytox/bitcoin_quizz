interface LNBitsConfig {
    adminKey: string;
    invoiceReadKey: string;
    endpoint?: string;
}
interface WalletDetails {
    id: string;
    name: string;
    balance: number;
}
interface CreateInvoice {
    checking_id: string;
    lnurl_response: string;
    payment_hash: string;
    payment_request: string;
}
interface PayInvoice {
    payment_hash: string;
}
interface CheckInvoice {
    payment_hash: string;
}
export declare class LNBitsWalletClass {
    private adminKey;
    private invoiceReadKey;
    private endpoint;
    private api;
    constructor(params: LNBitsConfig);
    walletDetails: () => Promise<WalletDetails>;
    createInvoice: (params?: {
        amount: number;
        memo: string;
        out?: boolean;
    }) => Promise<CreateInvoice>;
    payInvoice: (params?: {
        bolt11: string;
        out: boolean;
    }) => Promise<PayInvoice>;
    checkInvoice: (params: {
        payment_hash: string;
    }) => Promise<CheckInvoice>;
}
export {};
